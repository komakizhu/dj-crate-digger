interface:
  display_name: "老炮DJ (dj-crate-digger)"
  short_description: "老炮DJ：为 DJ 挖掘、验证、排序并编排适合场景的真实可播放歌单"
  default_prompt: "Use $dj-crate-digger to collect music-curation preferences interactively, then discover, verify, rank, and sequence music for a DJ set or playlist. During the two-round intake, reproduce the canonical template from SKILL.md literally, preserving every heading, punctuation mark, inline backtick, example, blank line, and the single fenced Markdown block; do not summarize or paraphrase it."
