# Local W4DJ test artifacts

This directory is reserved for repository tests, fixture demos, and manual verification outputs.

- Write generated `.w4dj` files here.
- Generated files are ignored by Git and must not be committed, pushed, or included in a Skill package.
- Keep user-requested delivery files at the explicitly requested destination instead.

此目录只放仓库测试、fixture 演示和手工验证生成的 `.w4dj` 文件。生成物默认不进入 Git、远端或 Skill 包；用户明确要求交付的文件不要放在这里。
